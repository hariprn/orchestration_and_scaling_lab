apiVersion: apps/v1
kind: Deployment

metadata:
  name: streaming-service
  namespace: streamingapp

spec:
  replicas: 2

  selector:
    matchLabels:
      app: streaming-service

  template:
    metadata:
      labels:
        app: streaming-service

    spec:
      containers:
      - name: streaming-service

        image: 610405653088.dkr.ecr.ap-south-1.amazonaws.com/streamingapp-streaming:latest

        ports:
        - containerPort: 3002

        env:
        - name: PORT
          value: "3002"

        - name: MONGO_URI
          value: "mongodb://mongo:27017/streamingapp"

        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: JWT_SECRET

---
apiVersion: v1
kind: Service

metadata:
  name: streaming-service
  namespace: streamingapp

spec:
  selector:
    app: streaming-service

  ports:
  - port: 3002
    targetPort: 3002
